interface:
  display_name: "SDLC Pipeline"
  short_description: "Plan and run the gated SDLC pipeline"
  default_prompt: "Use $sdlc to inspect the board and advance the next eligible task through the gated pipeline."
policy:
  allow_implicit_invocation: true

